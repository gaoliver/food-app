class Category {
  constructor(id, title, color, icon, dark) {
    this.id = id;
    this.title = title;
    this.color = color;
    this.icon = icon;
    this.dark = dark;
  }
}

export default Category;
